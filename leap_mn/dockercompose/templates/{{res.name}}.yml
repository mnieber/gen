version: "3.7"

services:
{% for service in res.services %}
{% if res.is_dev %}
  {{ service.docker_compose_dev_config.get_body() | to_nice_yaml }}
{% else %}
  {{ service.docker_compose_config.get_body() | to_nice_yaml }}
{% endif %}

{% endfor %}
