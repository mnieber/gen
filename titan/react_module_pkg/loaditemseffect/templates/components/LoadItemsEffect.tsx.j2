{% clean_up_js_imports %}
import React from 'react';
import { Effect, EffectWithoutArgs } from 'src/utils/components';
{{ sections.import_query() }}
{% end_clean_up_js_imports %}

{% if _.route_params %}
{{ sections.effect_args() }}
{% endif %}

type PropsT = {};

export const {{ res.name }}: React.FC<PropsT> = (p: PropsT) => {
  return (
{% if _.route_params %}
    <Effect
      {{ sections.effect() }}
    />
{% else %}
    <EffectWithoutArgs
      f={() => {
        {{ sections.effect_without_args() }}
      }}
    />
{% endif %}
  );
};
