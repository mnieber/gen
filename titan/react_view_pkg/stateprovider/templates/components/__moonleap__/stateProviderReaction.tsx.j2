      const cleanUpReaction = reaction(
        () => {
          return {
            {{ pipeline.output_name }}: {{ pipeline.result_expression }},                                       {% !! pipeline in __.pipelines %}
          }
        },
        (inputs) => {
          R.forEach(initRS, inputs.containerItems ?? []);                                                       {% for container in __.containers %}
          state.myContainer.data.containerItems = inputs.{{ __.get_pipeline(container).output_name }} ?? [];    {% endfor %}
          if (flags.logStateProviders) {
            log('MyState updated', state.getSummary());
          }
        },
        {
          fireImmediately: true,
        }
      );
      return () => {
        cleanUpReaction();
        state.destroy();
      };
