{
  "folders": [
    {
      "path": "{{ settings.references.src }}"
    },
    {                                           {% for path in res.paths %}
      "path": "{{ path }}"
    },                                          {% endfor %}
  ],
  "settings": {}
}