import graphene
from graphene.types.generic import GenericScalar
{{ sections.query_imports(mutation) }}

class {{ mutation.name | upper0 }}(graphene.Mutation):
{{ sections.endpoint_outputs(mutation) }}

    class Arguments:
{{ sections.endpoint_arguments(mutation) }}

{{ sections.mutate_function(mutation) }}
