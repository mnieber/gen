import graphene
from graphene.types.generic import GenericScalar
{{ sections.mutation_imports(mutation) }}

class {{ mutation.name | upper0 }}(graphene.Mutation):
{{ sections.mutation_outputs(mutation) }}

    class Arguments:
{{ sections.mutation_arguments(mutation) }}

{{ sections.mutate_function(mutation) }}
