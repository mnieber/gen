{% for item_name in res.item_names %}
export type {{ item_name|title }}T = {
    id: string,
    name: string
}

export type {{ item_name|title|byId }}T = {[id:string]: {{ item_name|title }}T};

{% endfor %}