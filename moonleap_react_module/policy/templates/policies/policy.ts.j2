{% magic_with res.name as yellowTulip %}
{% magic_with res.store.name as RedRose %}
import { reaction } from 'mobx';
import { RedRose } from '{{ res.store.module_path }}/RedRose';

export const yellowTulip = () => (redRose: RedRose) => {
  reaction(
    () => redRose,
    (redRose) => {
      // TODO
    },
    { fireImmediately: true }
  );
};
{% end_magic_with %}
{% end_magic_with %}